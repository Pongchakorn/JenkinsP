# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: 'vs2017-win2016'

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'

- script: |
    echo Add other tasks to build, test, and deploy your project.
    echo See https://aka.ms/yaml
  displayName: 'Run a multi-line script'

- task: katalonTask@0
  inputs:
    version: '7.1.12'
    executeArgs: '-projectPath="C:\Users\IT-Tester\Desktop\Tester\Automate Test\JenkinsP\JenkinsP.prj" -retry=0 -testSuitePath="Test Suites/JenkinsTest" -executionProfile="default" -browserType="Chrome" -apiKey="bb616c46-8ffe-4f25-9bb3-5cb3cccb4c4b"'