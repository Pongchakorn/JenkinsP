# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'

- script: |
    echo Add other tasks to build, test, and deploy your project.
    echo See https://aka.ms/yaml
  displayName: 'Run a multi-line script'

- script: |
    echo katalonc -noSplash -runMode=console -projectPath="C:\Users\IT-Tester\Desktop\Tester\Automate Test\JenkinsP\JenkinsP.prj" -retry=0 -testSuitePath="Test Suites/JenkinsTest" -executionProfile="default" -browserType="Chrome" -apiKey="d2c89fd3-17fa-4673-bb95-a98c9ab11b61"
    echo Finish
  displayName: 'Run a multi2-line script'